# Accuracy Limitations

**Q:** When does accuracy fail as a metric?

**A:**
- Fails with imbalanced classes (99% accuracy by predicting all majority)
- Doesn't distinguish between types of errors
- For imbalanced data: use precision, recall, F1 instead

**See also:** [[202202230714 Imbalanced Classification]]

---
