# Choosing a Loss Function

**Q:** What loss function do you use for each task type?
- Regression, Binary classification, Multi-class

**A:**

- Regression: MSE (default), MAE (outliers), Huber (both)
- Binary classification: binary cross-entropy + sigmoid
- Multi-class: categorical cross-entropy + softmax

---
