# Information Criteria for Model Selection

**Q:** Why use AIC/BIC for model selection?

**A:**

- No separate validation set needed (uses training data only)
- Compare models trained on the same data; lower score wins
- Useful when CV is expensive or dataset is small

---
