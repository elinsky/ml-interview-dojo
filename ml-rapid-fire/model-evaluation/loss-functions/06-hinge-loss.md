# Hinge Loss

**Q:** What is hinge loss and where is it used?

**A:**

- L = max(0, 1 - y·ŷ) where y ∈ {-1, +1}
- Used by Support Vector Machines
- Creates maximum margin classifier
- Zero loss when prediction correct with margin ≥ 1

---
