# Simple vs Multiple Linear Regression

**Q:** What is the difference between simple and multiple linear regression?

**A:** Simple: one input variable (y = b + mx). Multiple: two or more input variables (y = b + m1x1 + m2x2 + ...). Multiple regression fits a hyperplane instead of a line.

---
