# Decision Boundary

**Q:** What is the decision boundary in logistic regression?

**A:** A linear hyperplane where P(y=1|x) = 0.5.

Despite the nonlinear sigmoid, the decision boundary is linear because we threshold at sigmoid(0) = 0.5, which occurs when w^Tx + b = 0.

---
